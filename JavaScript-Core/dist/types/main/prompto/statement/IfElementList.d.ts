import ObjectList from "../utils/ObjectList";
import { IfElement } from "./index";
export default class IfElementList extends ObjectList<IfElement> {
    constructor(items?: IfElement[], item?: IfElement);
}
