define name as storable Text attribute
define value as Text attribute
define Entity as storable category with attributes name and value

define main as method doing:
	entity = Entity with "name" as name and "value" as value
	store entity
